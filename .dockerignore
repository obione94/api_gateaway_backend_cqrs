# Ignorer le dossier node_modules local (évite de copier les modules locaux dans l'image)
node_modules

# Ignorer le dossier de build TypeScript (dist) car on reconstruit dans l'image
dist

# Ignorer dossiers git et fichiers de configuration
.git
.gitignore

# Ignorer fichiers locaux d'environnement (ils seront gérés autrement)
.env
.env.local
.env.*.local

# Ignorer logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorer dossiers de cache, IDE, OS
.vscode
.idea
.DS_Store

# Ignorer fichiers temporaires
*.swp